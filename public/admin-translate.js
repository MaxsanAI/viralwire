const button=document.querySelector(".ai-translate");const status=document.querySelector("#translate-status");button?.addEventListener("click",async()=>{const storyId=button.getAttribute("data-story-id");const language=button.getAttribute("data-language");if(!storyId||!language)return;button.disabled=true;button.textContent="Translating…";if(status)status.textContent="Translating the article with Cloudflare AI…";try{const res=await fetch("/api/admin/translate",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({story_id:Number(storyId),language})});const data=await res.json();if(!res.ok)throw new Error(data.error||"Translation failed.");if(status)status.textContent=data.message||"Translation saved.";window.location.reload()}catch(error){if(status)status.textContent=error instanceof Error?error.message:"Translation failed.";button.disabled=false;button.textContent="AI Translate from English"}});