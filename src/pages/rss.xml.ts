import type {APIRoute} from "astro";
import {getPublishedStories} from "../lib/db";
const esc=(s:string)=>s.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;");
export const GET:APIRoute=async({locals})=>{const base=locals.runtime?.env?.SITE_URL||"https://viralwire.pulserapp.com";const stories=await getPublishedStories(locals,30);const items=stories.map(s=>"<item><title>"+esc(s.title)+"</title><link>"+base+"/story/"+s.slug+"</link><description>"+esc(s.excerpt)+"</description><pubDate>"+new Date(s.published_at).toUTCString()+"</pubDate></item>").join("");return new Response('<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>VIRALWIRE</title><link>'+base+'</link><description>The stories taking over the internet.</description>'+items+"</channel></rss>",{headers:{"Content-Type":"application/rss+xml"}})};